    <form accept-charset="utf-8" action="" enctype="multipart/form-data" method="get" spellcheck="false">

        <fieldset class="box has-text-centered">

          {{# arguments }}
            {{> arguments }}
          {{/ arguments }}

          {{^ single-argument  }}
            {{> submit-button }}
          {{/ single-argument  }}

        </fieldset>

        {{# has-options }}
        <details class="options has-text-right"{{# has-option-selected}} open{{/ has-option-selected}}>

          <summary class="button is-info is-outlined is-centered">
            Options
          </summary>

          <div class="box has-text-left">
            {{# flags }}
              {{> flags }}
            {{/ flags }}

            {{# options }}
              {{> options }}
            {{/ options }}
          </div>

        </details>
        {{/ has-options }}

    </form>
